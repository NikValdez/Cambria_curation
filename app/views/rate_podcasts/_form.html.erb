<%= form_for(rate_podcast) do |f| %>
  <% if rate_podcast.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rate_podcast.errors.count, "error") %> prohibited this rate_podcast from being saved:</h2>

      <ul>
      <% rate_podcast.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :rating %>
    <%= f.number_field :rating %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
