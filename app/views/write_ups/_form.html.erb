<%= form_for(write_up) do |f| %>
  <% if write_up.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(write_up.errors.count, "error") %> prohibited this write_up from being saved:</h2>

      <ul>
      <% write_up.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="field">
    <%= f.label :image %>
    <%= f.file_field :image %>
  </div>

  
  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :content %>
    <%= f.text_area :content %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


